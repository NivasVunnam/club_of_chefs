[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes (e.g. /login) so client-side routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200